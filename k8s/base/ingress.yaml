apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ha-app
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ha-app
                port:
                  name: http
